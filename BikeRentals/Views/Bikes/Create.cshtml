@model BikeRentals.models.Bike

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Bike</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="BikeStatus" class="control-label"></label>
                <select asp-for="BikeStatus" class ="form-control" asp-items="ViewBag.BikeStatus"></select>
            </div>
            <div class="form-group">
                <label asp-for="BikeSize" class="control-label"></label>
                <select asp-for="BikeSize" class ="form-control" asp-items="ViewBag.BikeSize"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
