@model IEnumerable<BikeRentals.models.Bike>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BikeId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BikeSize)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BikeStatus)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BikeId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BikeSizeNavigation.BikeSize1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BikeStatusNavigation.BikeStats)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BikeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BikeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BikeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
